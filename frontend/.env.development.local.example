# Local development example
REACT_APP_API_ORIGIN=http://127.0.0.1:8787

# Optional: inject admin token for write actions in dev
# Requires matching ADMIN_TOKEN for the Worker in dev
REACT_APP_ADMIN_TOKEN=devtoken

